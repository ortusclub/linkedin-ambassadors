{
  "buildCommand": "npx prisma generate && npm run build",
  "framework": "nextjs",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/detect-deposits",
      "schedule": "* * * * *"
    }
  ]
}
