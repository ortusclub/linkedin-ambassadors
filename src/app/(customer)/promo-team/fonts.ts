import { Poppins } from "next/font/google";

// Display face for the Promo Team page — matches the LinkedVelocity design system.
export const poppins = Poppins({
  subsets: ["latin"],
  weight: ["600", "700", "800"],
  display: "swap",
});
